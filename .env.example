# Track Rack — Demucs stem separator (local dev only unless you deploy workers/demucs-api separately)
# Not available on Render static site deploy — Audio → MIDI works fully in-browser without this.
# VITE_STEM_API_URL=http://localhost:8765
